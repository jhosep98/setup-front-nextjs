import type { NextPage } from 'next';
import { Typography } from '@mui/material';

const Home: NextPage = () => (
  <main>
    <Typography>Setup Front Nextjs</Typography>
  </main>
);

export default Home;
